spring:
  application:
    name: securevault-backend

  data:
    mongodb:
      uri: mongodb+srv://securevaultauth_db_user1:Shreya2019@cluster0.zhxojsi.mongodb.net/securevault?retryWrites=true&w=majority

  jackson:
    time-zone: UTC

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

  # ✅ EMAIL CONFIGURATION - ADD THIS SECTION
  mail:
    host: smtp.gmail.com
    port: 587
    username: securevault.auth@gmail.com  # Replace with your Gmail
    password: wxqgzxcwueepceai  # Replace with Gmail App Password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    test-connection: false

server:
  port: 8080
  servlet:
    context-path: /api

jwt:
  secret: your-super-secret-jwt-key-change-in-production-at-least-32-characters-long
  expiration: 86400000

cors:
  allowed-origins: http://localhost:3000,http://localhost:3001
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS,PATCH
  allowed-headers: Content-Type,Authorization
  max-age: 3600

app:
  name: SecureVault
  version: 1.0.0
  inactivity:
    check-interval: 1h
    grace-period-days: 7

# ✅ OTP CONFIGURATION - ADD THIS SECTION
otp:
  expiry-minutes: 5
  length: 6

logging:
  level:
    org.springframework.security: INFO
    org.springframework.data.mongodb: DEBUG
    org.springframework.mail: DEBUG
